---
phase: 01-stability-and-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - providers/base.py
  - providers/manager.py
  - providers/openai_provider.py
  - providers/anthropic_provider.py
  - providers/gemini_provider.py
  - providers/groq_provider.py
  - providers/azure_openai_provider.py
  - providers/openrouter_provider.py
  - providers/openai_compat_provider.py
  - search_backends/base.py
  - search_backends/paapi_backend.py
  - search_backends/rapidapi_backend.py
  - search_backends/dataforseo_backend.py
  - search_backends/playwright_backend.py
  - search_backends/brightdata_backend.py
  - style.py
  - tests/test_providers_manager.py
  - tests/test_style.py
  - tests/test_search_timeout.py
autonomous: true
requirements:
  - STAB-01
  - STAB-02
  - STAB-07

must_haves:
  truths:
    - "A vision provider that hangs for >30s is terminated (individual timeout)"
    - "Total vision analysis across all fallback attempts completes within 60s"
    - "A provider disabled after repeated failures automatically recovers after the configured cooldown"
    - "Regular users see friendly error messages with no technical details"
    - "Admin users see provider name and admin panel link in error messages"
    - "All search backends enforce a 15s timeout per user decision"
  artifacts:
    - path: "providers/base.py"
      provides: "PROVIDER_TIMEOUT_SECONDS = 30"
      contains: "PROVIDER_TIMEOUT_SECONDS = 30"
    - path: "providers/manager.py"
      provides: "60s total deadline in analyse_image"
      contains: "deadline"
    - path: "search_backends/base.py"
      provides: "SEARCH_TIMEOUT_SECONDS = 15"
      contains: "SEARCH_TIMEOUT_SECONDS = 15"
    - path: "style.py"
      provides: "Admin vs user error differentiation"
      contains: "is_admin"
    - path: "tests/test_providers_manager.py"
      provides: "Timeout enforcement tests"
      contains: "timeout"
    - path: "tests/test_style.py"
      provides: "Error message differentiation tests"
      contains: "error"
    - path: "tests/test_search_timeout.py"
      provides: "Search backend timeout tests"
      contains: "SEARCH_TIMEOUT_SECONDS"
  key_links:
    - from: "providers/manager.py"
      to: "providers/base.py"
      via: "PROVIDER_TIMEOUT_SECONDS import"
      pattern: "PROVIDER_TIMEOUT_SECONDS"
    - from: "providers/manager.py"
      to: "each provider's analyse()"
      via: "asyncio.wait_for with deadline-aware timeout"
      pattern: "asyncio\\.wait_for"
    - from: "search_backends/paapi_backend.py"
      to: "search_backends/base.py"
      via: "SEARCH_TIMEOUT_SECONDS import"
      pattern: "SEARCH_TIMEOUT_SECONDS"
---

<objective>
Unify vision provider timeouts, unify search backend timeouts, tune health tracking recovery, and differentiate error messages for admin vs regular users.

Purpose: Eliminate hung requests from slow providers/backends and ensure the bot always responds within a predictable time window. Make error messages useful for admins while keeping them friendly for regular users.
Output: All 7+ provider files updated with unified 30s timeout, manager enforces 60s total cap, all search backends use 15s timeout, style.py error functions differentiate admin/user messages.
</objective>

<execution_context>
@C:/Users/leofu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leofu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stability-and-infrastructure/01-CONTEXT.md
@.planning/phases/01-stability-and-infrastructure/01-RESEARCH.md

@providers/base.py
@providers/manager.py
@search_backends/base.py
@search_backends/paapi_backend.py
@search_backends/rapidapi_backend.py
@search_backends/dataforseo_backend.py
@search_backends/playwright_backend.py
@search_backends/brightdata_backend.py
@style.py
@tests/test_providers_manager.py
@tests/test_style.py

<interfaces>
<!-- Key constants and functions the executor needs -->

From providers/base.py:
```python
PROVIDER_TIMEOUT_SECONDS = 60  # Change to 30
```

From providers/manager.py:
```python
async def analyse_image(...):  # Add 60s total deadline
async def _safe_run(provider: VisionProvider) -> Optional[ProviderResult]:  # Uses asyncio.wait_for
_failures_in_window(failure_timestamps, window_seconds) -> int
async def _handle_progressive_health(...)
# Config values used: config.HEALTH_FAILURE_WINDOW, config.HEALTH_DISABLE_THRESHOLD, config.HEALTH_RECOVERY_COOLDOWN
```

From search_backends/base.py:
```python
# No timeout constant exists yet -- add SEARCH_TIMEOUT_SECONDS = 15
class SearchBackend(ABC):
    async def search(self, query: str, max_results: int, page: int = 1) -> list[AmazonItem]: ...
```

From search_backends (current timeout values to unify):
```python
# paapi_backend.py:141    -> aiohttp.ClientTimeout(total=15)  # already 15s
# rapidapi_backend.py:109 -> aiohttp.ClientTimeout(total=15)  # already 15s
# dataforseo_backend.py:78,114,119 -> aiohttp.ClientTimeout(total=10) and (total=30)  # inconsistent
# playwright_backend.py:219 -> page.goto(timeout=30_000)  # 30s page load
# brightdata_backend.py:68 -> aiohttp.ClientTimeout(total=30)  # 30s
```

From style.py:
```python
def error_no_providers(is_admin: bool = False) -> str:
def error_analysis_failed(is_admin: bool = False) -> str:
def error_no_backend() -> str:  # No is_admin param yet
def error_no_results() -> str:  # No is_admin param yet
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify provider timeouts and add total deadline cap</name>
  <files>providers/base.py, providers/manager.py, providers/openai_provider.py, providers/anthropic_provider.py, providers/gemini_provider.py, providers/groq_provider.py, providers/azure_openai_provider.py, providers/openrouter_provider.py, providers/openai_compat_provider.py, tests/test_providers_manager.py</files>
  <behavior>
    - Test: A provider that takes >30s is cancelled and raises TimeoutError
    - Test: When multiple providers are tried in fallback, total time never exceeds 60s
    - Test: Second provider gets reduced timeout = 60s minus time already spent
    - Test: Health tracking recovery -- a disabled provider is re-enabled after HEALTH_RECOVERY_COOLDOWN seconds pass
  </behavior>
  <action>
1. In `providers/base.py`: Change `PROVIDER_TIMEOUT_SECONDS = 60` to `PROVIDER_TIMEOUT_SECONDS = 30`.

2. In `providers/manager.py` `analyse_image()`:
   - Compute a deadline: `deadline = asyncio.get_event_loop().time() + 60` at function start.
   - Modify `_safe_run()` to accept the deadline and compute remaining time: `remaining = max(1.0, deadline - asyncio.get_event_loop().time())`. Use `min(PROVIDER_TIMEOUT_SECONDS, remaining)` as the timeout for `asyncio.wait_for()`.
   - Remove the hardcoded `recovery_timeout=60.0` parameter if it exists and replace with deadline-aware logic.

3. In each provider file (`openai_provider.py`, `anthropic_provider.py`, `gemini_provider.py`, `groq_provider.py`, `azure_openai_provider.py`, `openrouter_provider.py`, `openai_compat_provider.py`):
   - Find the HTTP client timeout configuration (e.g., `httpx.Timeout(60)` or `aiohttp timeout`).
   - Change it to use `PROVIDER_TIMEOUT_SECONDS` (30s) consistently.
   - Remove any inner `asyncio.wait_for()` calls if the provider wraps its own call -- the manager handles this. If the provider's own `analyse()` method has `asyncio.wait_for`, remove it and let the manager's `_safe_run` be the single timeout enforcement point.

4. Verify STAB-02 (health tracking recovery): Read `_handle_progressive_health()` and confirm auto-recovery path works. The existing test in `test_progressive_health.py` should cover this. If `HEALTH_RECOVERY_COOLDOWN` defaults are sensible (600s = 10min), leave them. Add a brief comment documenting the recovery behavior.

5. Add tests to `tests/test_providers_manager.py`:
   - `test_provider_timeout_30s`: Mock a provider that sleeps 35s, assert it is cancelled within ~30s.
   - `test_total_deadline_60s`: Mock two providers (first takes 25s, second takes 40s), assert total time < 61s and second provider gets cancelled.
   - `test_deadline_remaining_passed_to_fallback`: Mock first provider failing at 20s, assert second provider's timeout is ~40s (60-20).
  </action>
  <verify>
    <automated>cd C:/projects/amazon-photo-bot && pytest tests/test_providers_manager.py -x -k timeout --timeout=30</automated>
  </verify>
  <done>PROVIDER_TIMEOUT_SECONDS is 30, all provider files use it consistently, no dual-timeout patterns remain, manager enforces 60s total cap, 3+ timeout tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unify search backend timeouts to 15 seconds</name>
  <files>search_backends/base.py, search_backends/paapi_backend.py, search_backends/rapidapi_backend.py, search_backends/dataforseo_backend.py, search_backends/playwright_backend.py, search_backends/brightdata_backend.py, tests/test_search_timeout.py</files>
  <behavior>
    - Test: SEARCH_TIMEOUT_SECONDS is 15 in search_backends/base.py
    - Test: Each backend's HTTP client timeout references SEARCH_TIMEOUT_SECONDS
    - Test: Playwright backend's page.goto timeout is 15000ms (matching SEARCH_TIMEOUT_SECONDS * 1000)
  </behavior>
  <action>
1. In `search_backends/base.py`: Add `SEARCH_TIMEOUT_SECONDS = 15` constant near the top of the file, after imports. Add a comment: `# Unified timeout for all search backends (per user decision)`.

2. In `search_backends/paapi_backend.py`:
   - Import `SEARCH_TIMEOUT_SECONDS` from `search_backends.base`.
   - Replace `aiohttp.ClientTimeout(total=15)` (line 141) with `aiohttp.ClientTimeout(total=SEARCH_TIMEOUT_SECONDS)`.

3. In `search_backends/rapidapi_backend.py`:
   - Import `SEARCH_TIMEOUT_SECONDS` from `search_backends.base`.
   - Replace `aiohttp.ClientTimeout(total=15)` (line 109) with `aiohttp.ClientTimeout(total=SEARCH_TIMEOUT_SECONDS)`.

4. In `search_backends/dataforseo_backend.py`:
   - Import `SEARCH_TIMEOUT_SECONDS` from `search_backends.base`.
   - Replace all `aiohttp.ClientTimeout(total=10)` and `aiohttp.ClientTimeout(total=30)` occurrences (lines 78, 114, 119) with `aiohttp.ClientTimeout(total=SEARCH_TIMEOUT_SECONDS)`.

5. In `search_backends/playwright_backend.py`:
   - Import `SEARCH_TIMEOUT_SECONDS` from `search_backends.base`.
   - Replace `timeout=30_000` in `page.goto()` (line 219) with `timeout=SEARCH_TIMEOUT_SECONDS * 1000`.
   - Replace `timeout=10_000` (line 235) with `timeout=SEARCH_TIMEOUT_SECONDS * 1000`.
   - Replace `timeout=10` in `asyncio.wait_for` (line 246) with `timeout=SEARCH_TIMEOUT_SECONDS`.

6. In `search_backends/brightdata_backend.py`:
   - Import `SEARCH_TIMEOUT_SECONDS` from `search_backends.base`.
   - Replace `aiohttp.ClientTimeout(total=30)` (line 68) with `aiohttp.ClientTimeout(total=SEARCH_TIMEOUT_SECONDS)`.

7. Create `tests/test_search_timeout.py`:
   - `test_search_timeout_constant_is_15`: Assert `SEARCH_TIMEOUT_SECONDS == 15`.
   - `test_backends_import_timeout_constant`: For each backend module, import it and grep or inspect that it references `SEARCH_TIMEOUT_SECONDS` (use `inspect.getsource()` to verify no hardcoded timeout values remain).
  </action>
  <verify>
    <automated>cd C:/projects/amazon-photo-bot && pytest tests/test_search_timeout.py -x --timeout=30</automated>
  </verify>
  <done>SEARCH_TIMEOUT_SECONDS = 15 in search_backends/base.py, all 5 backend files import and use it instead of hardcoded values, tests verify the constant and its usage</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Differentiate error messages for admin vs regular users</name>
  <files>style.py, tests/test_style.py</files>
  <behavior>
    - Test: error_no_providers(is_admin=False) returns friendly message without provider names
    - Test: error_no_providers(is_admin=True) returns message with admin panel link
    - Test: error_analysis_failed(is_admin=False) returns "We couldn't analyze your photo right now. Please try again in a few minutes."
    - Test: error_analysis_failed(is_admin=True) includes provider name hint and admin panel link
    - Test: error_no_results(is_admin=False) does NOT mention "Amazon"
    - Test: error_no_backend(is_admin=True) includes which backend failed
  </behavior>
  <action>
1. In `style.py`, update error functions per CONTEXT.md locked decisions:
   - `error_analysis_failed(is_admin=False)`: Return "We couldn't analyze your photo right now. Please try again in a few minutes." (MarkdownV2 escaped). No technical details.
   - `error_analysis_failed(is_admin=True)`: Keep current behavior (provider name + admin panel link).
   - `error_no_results(is_admin=False)`: "We couldn't find matching products. Try a clearer photo or a different angle." Do NOT mention Amazon.
   - `error_no_results(is_admin=True)`: Add `is_admin` parameter, show which search backend was attempted.
   - `error_no_backend()`: Add `is_admin` parameter. Regular user: generic message. Admin: which backend(s) are unavailable.
   - `error_no_providers()`: Already has `is_admin` -- verify it follows the pattern (friendly for users, detailed for admins).

2. Add tests to `tests/test_style.py`:
   - Test each error function with `is_admin=True` and `is_admin=False`.
   - Assert user-facing messages do NOT contain "Amazon", provider names, or technical terms.
   - Assert admin-facing messages contain "admin" or "/admin" reference.
   - Assert user-facing `error_analysis_failed` matches exact CONTEXT.md wording.
  </action>
  <verify>
    <automated>cd C:/projects/amazon-photo-bot && pytest tests/test_style.py -x -k error --timeout=30</automated>
  </verify>
  <done>All error functions accept is_admin parameter, user messages are friendly with no technical details, admin messages include provider/backend info and admin panel link, no error message mentions "Amazon" to regular users</done>
</task>

</tasks>

<verification>
```bash
cd C:/projects/amazon-photo-bot && pytest tests/test_providers_manager.py tests/test_style.py tests/test_progressive_health.py tests/test_search_timeout.py -x --timeout=30
```
</verification>

<success_criteria>
- PROVIDER_TIMEOUT_SECONDS is 30 in providers/base.py
- SEARCH_TIMEOUT_SECONDS is 15 in search_backends/base.py
- No provider file has dual-timeout patterns (both client timeout and asyncio.wait_for)
- No search backend file has hardcoded timeout values (all use SEARCH_TIMEOUT_SECONDS)
- analyse_image() enforces 60s total deadline across all fallback attempts
- All error_* functions in style.py differentiate admin vs regular user messages
- User error messages match CONTEXT.md locked decisions exactly
- All new and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-stability-and-infrastructure/01-01-SUMMARY.md`
</output>
