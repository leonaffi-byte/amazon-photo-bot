---
phase: 03-web-admin-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - admin_dashboard/__init__.py
  - admin_dashboard/router.py
  - admin_dashboard/auth.py
  - admin_dashboard/deps.py
  - admin_dashboard/sparklines.py
  - admin_dashboard/templates/base.html
  - admin_dashboard/templates/login.html
  - admin_dashboard/templates/home.html
  - admin_dashboard/templates/partials/stat_cards.html
  - admin_dashboard/templates/partials/provider_health.html
  - database.py
  - gateway.py
  - config.py
  - requirements.txt
  - tests/test_admin_web.py
autonomous: true
requirements:
  - ADMN-01
  - ADMN-02

must_haves:
  truths:
    - "GET /admin/ redirects to /admin/login when no session cookie exists"
    - "Telegram Login Widget HMAC-SHA256 verification accepts valid data and rejects tampered or stale (>24h) data"
    - "Fallback token generated at startup is accepted by /admin/login and expires after 24h"
    - "Authenticated GET /admin/ returns 200 with stat cards showing user count, searches today/week/month, photos analyzed"
    - "HTMX polling endpoint GET /admin/partials/stats returns HTML fragment (not full page) with stat card data"
    - "Sparkline SVG generation produces valid polyline SVG string for 7-day data with graceful flat-line fallback"
  artifacts:
    - path: "admin_dashboard/__init__.py"
      provides: "Module root exporting router"
    - path: "admin_dashboard/router.py"
      provides: "FastAPI APIRouter with home, login, callback, logout, partial stats routes"
      exports: ["router"]
    - path: "admin_dashboard/auth.py"
      provides: "Telegram Login Widget HMAC verification + fallback token"
      exports: ["verify_telegram_login", "generate_fallback_token", "verify_fallback_token"]
    - path: "admin_dashboard/deps.py"
      provides: "require_admin() FastAPI dependency — redirects to /admin/login if no session"
      exports: ["require_admin"]
    - path: "admin_dashboard/sparklines.py"
      provides: "Server-side inline SVG sparkline from list[int]"
      exports: ["points_to_svg", "build_7day_sparkline"]
    - path: "admin_dashboard/templates/base.html"
      provides: "Tailwind CDN + HTMX CDN layout with sidebar, hamburger, content block"
    - path: "admin_dashboard/templates/login.html"
      provides: "Login page with Telegram widget + fallback token form"
    - path: "admin_dashboard/templates/home.html"
      provides: "Dashboard home with stat card section and polling div"
    - path: "admin_dashboard/templates/partials/stat_cards.html"
      provides: "HTMX-pollable stat card grid with outerHTML hx-swap"
    - path: "admin_dashboard/templates/partials/provider_health.html"
      provides: "HTMX-pollable provider health table with outerHTML hx-swap"
    - path: "database.py"
      provides: "get_daily_search_counts(days) async helper returning list[int]"
    - path: "gateway.py"
      provides: "Admin router mounted at /admin with SessionMiddleware"
    - path: "tests/test_admin_web.py"
      provides: "Test stubs for all ADMN-01 through ADMN-06"
  key_links:
    - from: "gateway.py"
      to: "admin_dashboard/router.py"
      via: "app.include_router(admin_router, prefix='/admin') before shortener catch-all"
    - from: "admin_dashboard/router.py"
      to: "admin_dashboard/deps.py"
      via: "Depends(require_admin) on every route except /login and /auth/callback"
    - from: "admin_dashboard/templates/home.html"
      to: "GET /admin/partials/stats"
      via: "hx-get='/admin/partials/stats' hx-trigger='every 60s' hx-swap='outerHTML'"
---

<objective>
Build the admin_dashboard module scaffold, authentication system, and dashboard home page with statistics.

Purpose: Admins need browser-based access to the bot dashboard. This plan establishes the entire module structure, auth flow (Telegram widget + fallback token), session management, and the stats home page with HTMX polling. All subsequent plans in this phase build on these foundations.
Output: Working /admin route with login and authenticated home page showing stat cards.
</objective>

<execution_context>
@C:/Users/leofu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leofu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-admin-dashboard/03-CONTEXT.md
@.planning/phases/03-web-admin-dashboard/03-RESEARCH.md
@gateway.py
@admin_service.py
@database.py
@config.py
</context>

<interfaces>
<!-- Key types and contracts from existing codebase. No codebase exploration needed. -->

From admin_service.py:
```python
@dataclass
class BotStats:
    total_users: int
    searches_today: int
    searches_week: int
    searches_month: int
    photos_analyzed: int
    products_found: int

@dataclass
class ProviderHealth:
    name: str
    status: str          # "healthy" | "degraded" | "disabled"
    success_rate: float
    avg_latency_ms: float
    failure_count: int

async def get_stats() -> BotStats: ...
async def get_provider_health() -> list[ProviderHealth]: ...
async def is_admin(user_id: int) -> bool: ...
```

From database.py:
```python
async def get_stats_since(since: datetime) -> dict:
    """Returns dict with keys: total_users, total_searches, photo_searches"""
```

From gateway.py (create_app function — current route order):
```python
# 1. app.include_router(api_router)         # /api/v1/*
# 2. app.include_router(webhook_router)     # /webhook/* (conditional)
# 3. app.include_router(shortener_router)   # /{code} CATCH-ALL — MUST STAY LAST
```

From config.py:
```python
TELEGRAM_BOT_TOKEN: str  # used as HMAC secret key for Telegram Login Widget
ADMIN_IDS: set[int]      # bootstrap admin list
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module scaffold, auth system, database helper, and test stubs</name>
  <files>
    admin_dashboard/__init__.py,
    admin_dashboard/auth.py,
    admin_dashboard/deps.py,
    admin_dashboard/sparklines.py,
    database.py,
    requirements.txt,
    tests/test_admin_web.py
  </files>
  <behavior>
    - verify_telegram_login(data, bot_token) returns True for valid HMAC data with auth_date within 24h
    - verify_telegram_login returns False for tampered data (wrong hash)
    - verify_telegram_login returns False for stale data (auth_date > 24h ago)
    - generate_fallback_token() returns a non-empty urlsafe string and updates module state
    - verify_fallback_token(token) returns True for the current token within 24h
    - verify_fallback_token returns False for a wrong token
    - verify_fallback_token returns False when token is older than 24h
    - points_to_svg([10, 20, 15, 30, 5, 25, 18]) returns a string starting with `<svg`
    - points_to_svg([]) returns a flat-line SVG (no crash)
    - points_to_svg([0, 0, 0, 0, 0, 0, 0]) returns a flat-line SVG
    - get_daily_search_counts(days=7) returns a list of 7 ints (one per day, newest last)
  </behavior>
  <action>
1. Create `admin_dashboard/__init__.py` — empty file that makes the directory a package. Add `from .router import router` import (router.py created in Task 2).

2. Create `admin_dashboard/auth.py`:
   - `verify_telegram_login(data: dict, bot_token: str) -> bool`: Pop `hash` from a copy of data, check `auth_date` freshness (< 86400s from now), build sorted `key=value\n` check string, compute `secret_key = hashlib.sha256(bot_token.encode()).digest()`, compute expected HMAC with `hmac.new(secret_key, check_string.encode(), hashlib.sha256).hexdigest()`, return `hmac.compare_digest(expected, received)`. NOTE: use a copy of `data` dict before popping hash so original is not mutated.
   - Module-level `_fallback_token: str | None = None` and `_token_issued_at: float = 0`
   - `generate_fallback_token() -> str`: `secrets.token_urlsafe(32)`, update module globals, return token
   - `verify_fallback_token(token: str) -> bool`: return False if no token, if `time.time() - _token_issued_at > 86400`, or if `not hmac.compare_digest(token, _fallback_token)`

3. Create `admin_dashboard/deps.py`:
   - `async def require_admin(request: Request) -> int`: Check `request.session.get("admin_user_id")`; if missing, return `RedirectResponse("/admin/login", status_code=302)` — use `from fastapi.responses import RedirectResponse` and raise it via `from fastapi import HTTPException` or just return the response. Actually: FastAPI dependencies that return responses work differently — raise an HTTPException that causes the redirect OR use a custom approach. Correct pattern: if no session, raise `HTTPException(status_code=307, headers={"Location": "/admin/login"})`. Return `int(user_id)`.

4. Create `admin_dashboard/sparklines.py`:
   - `points_to_svg(values: list[int], width: int = 60, height: int = 20) -> str`: If values is empty or all-zero, return flat SVG polyline at mid-height. Otherwise normalize: `mn, mx = min(values), max(values); rng = mx - mn or 1`. Compute x/y coords for each point: `x = i * (width / (len(values)-1)); y = height - ((v-mn)/rng)*(height-4) - 2`. Return `<svg width="{width}" height="{height}" viewBox="0 0 {width} {height}"><polyline points="{pts}" fill="none" stroke="#6366f1" stroke-width="1.5"/></svg>`.
   - `async def build_7day_sparkline(width=60, height=20) -> str`: Call `get_daily_search_counts(days=7)` from database and pass to `points_to_svg`.

5. Add `get_daily_search_counts(days: int = 7) -> list[int]` to `database.py`:
   - Uses GROUP BY `date(searched_at)` to get daily search counts for the last `days` days.
   - Returns a list of `days` ints ordered oldest-to-newest, filling 0 for days with no data.
   - SQL: `SELECT date(searched_at) as day, COUNT(*) FROM search_logs WHERE searched_at >= ? GROUP BY day ORDER BY day` where the cutoff is `datetime.now(timezone.utc) - timedelta(days=days)`.
   - Build dict from results, then fill list for each of the last `days` dates (using `(datetime.now(timezone.utc) - timedelta(days=i)).strftime("%Y-%m-%d")` iterated from `days-1` down to 0).

6. Add `python-multipart` and `itsdangerous` to `requirements.txt` if not present (check first — starlette is already there, itsdangerous is Starlette's dep but ensure it's explicit).

7. Create `tests/test_admin_web.py` with test stubs for ALL ADMN-01 through ADMN-06 requirements per the validation strategy. Import `AsyncClient` from `httpx` and `pytest`. Create a `@pytest.fixture` `admin_client` that builds the FastAPI test app with a pre-seeded admin session. Write test functions for: auth HMAC valid/tampered/stale, fallback token valid/expired, sparkline generation, home page 200/redirect, partial stats fragment check, keys page, save key, key masking, tags page, activate tag, add tag, settings page, update setting, health page, partial health. Mark integration tests that need the full app with `@pytest.mark.asyncio`. Use `unittest.mock.AsyncMock` to mock `admin_service` calls. Tests can be `pytest.mark.skip("Wave 0 stub")` initially — they must exist so imports are verifiable.
  </action>
  <verify>
    <automated>pytest tests/test_admin_web.py -x -q 2>&1 | head -30</automated>
  </verify>
  <done>
    - admin_dashboard/__init__.py, auth.py, deps.py, sparklines.py exist and are importable
    - verify_telegram_login accepts/rejects based on HMAC + age
    - generate_fallback_token / verify_fallback_token work correctly
    - points_to_svg returns valid SVG strings including edge cases
    - get_daily_search_counts added to database.py and returns list[int] of length `days`
    - tests/test_admin_web.py exists with test stubs for all ADMN requirements (may skip initially)
    - pytest collects all tests without import errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Router, templates, SessionMiddleware, and gateway wiring</name>
  <files>
    admin_dashboard/router.py,
    admin_dashboard/templates/base.html,
    admin_dashboard/templates/login.html,
    admin_dashboard/templates/home.html,
    admin_dashboard/templates/partials/stat_cards.html,
    admin_dashboard/templates/partials/provider_health.html,
    gateway.py,
    config.py
  </files>
  <action>
1. Add `ADMIN_SESSION_SECRET` to `config.py`:
   ```python
   import secrets as _secrets
   ADMIN_SESSION_SECRET: str = os.getenv(
       "ADMIN_SESSION_SECRET",
       _secrets.token_hex(32)  # random default, logged as warning if not set
   )
   ```
   After definition, add: `if not os.getenv("ADMIN_SESSION_SECRET"): logger.warning("ADMIN_SESSION_SECRET not set — using ephemeral random secret. Sessions will not survive restarts. Set ADMIN_SESSION_SECRET in .env.")`

2. Create `admin_dashboard/router.py`:
   - Module-level: `router = APIRouter(tags=["admin"])` and `templates = Jinja2Templates(directory=str(Path(__file__).parent / "templates"))`
   - Routes:
     - `GET /login` — render login.html with `bot_username` from config (strip `@` if present from bot_username; derive from `TELEGRAM_BOT_TOKEN` by calling BotFather or just read `TELEGRAM_BOT_USERNAME` env var with empty default). Pass `has_fallback=True` to show both auth options.
     - `GET /auth/callback` — query params include Telegram widget fields (`id`, `first_name`, `auth_date`, `hash`, etc.). Call `verify_telegram_login(dict(request.query_params), config.TELEGRAM_BOT_TOKEN)`. If valid and `await admin_service.is_admin(int(data["id"]))`, set `request.session["admin_user_id"] = data["id"]` and redirect to `/admin/`. Else redirect to `/admin/login?error=unauthorized`.
     - `POST /auth/token` (form) — accept `token: str = Form(...)`. Call `verify_fallback_token(token)`. If valid, set `request.session["admin_user_id"] = "0"` (fallback user — no real Telegram ID; session only). Redirect to `/admin/`. Else redirect to `/admin/login?error=invalid_token`.
     - `GET /logout` — clear `request.session`, redirect to `/admin/login`.
     - `GET /` — `Depends(require_admin)` — call `admin_service.get_stats()` and `admin_service.get_provider_health()` and `sparklines.build_7day_sparkline()`, render home.html.
     - `GET /partials/stats` — `Depends(require_admin)` — try/except around `admin_service.get_stats()` + `sparklines.build_7day_sparkline()`, render `partials/stat_cards.html`. On exception render degraded partial with error message.
     - `GET /partials/health` — `Depends(require_admin)` — try/except around `admin_service.get_provider_health()`, render `partials/provider_health.html`. On exception render degraded partial.

3. Create `admin_dashboard/templates/base.html`:
   - Full HTML5 boilerplate with Tailwind CDN (`<script src="https://cdn.tailwindcss.com"></script>`) and HTMX CDN (`<script src="https://unpkg.com/htmx.org@2.0.3/dist/htmx.min.js"></script>`).
   - Sidebar: fixed left, `w-64`, `hidden md:block`. Nav links: Dashboard (`/admin/`), Tags (`/admin/tags`), API Keys (`/admin/keys`), Settings (`/admin/settings`), Health (`/admin/health`), Logout (`/admin/logout`).
   - Mobile hamburger button: `md:hidden fixed top-4 left-4 z-50`, uses HTMX to toggle sidebar visibility via `hx-get="/admin/partials/sidebar-toggle" hx-target="#sidebar" hx-swap="outerHTML"`.
   - Main content area: `md:ml-64 p-6`. `{% block content %}{% endblock %}`.
   - `{% block title %}Admin{% endblock %}` in `<title>`.

4. Create `admin_dashboard/templates/login.html`:
   - Extends base.html. Centered card layout.
   - Telegram Login Widget `<script>` block with `data-telegram-login="{{ bot_username }}"` and `data-auth-url="{{ request.url_for('telegram_callback') }}"` (use named route).
   - Divider "OR" between widget and fallback form.
   - Fallback form: `<form method="POST" action="/admin/auth/token">` with password input for token, submit button "Login with token".
   - Show error message if `request.query_params.get("error")` is set.

5. Create `admin_dashboard/templates/home.html`:
   - Extends base.html. Title block: "Dashboard".
   - Top section: `<div id="stat-cards" hx-get="/admin/partials/stats" hx-trigger="every 60s" hx-swap="outerHTML">{% include "partials/stat_cards.html" %}</div>`.
   - Bottom section: `<div id="provider-health" hx-get="/admin/partials/health" hx-trigger="every 30s" hx-swap="outerHTML">{% include "partials/provider_health.html" %}</div>`.

6. Create `admin_dashboard/templates/partials/stat_cards.html`:
   - Must emit the outer `<div id="stat-cards" hx-get="/admin/partials/stats" hx-trigger="every 60s" hx-swap="outerHTML">` wrapper with HTMX attributes so polling continues after swap.
   - Inside: 3-column grid on lg, 1-column on mobile. Cards for: Total Users (`stats.total_users`), Searches Today (`stats.searches_today`), Searches This Week (`stats.searches_week`), Searches This Month (`stats.searches_month`), Photos Analyzed (`stats.photos_analyzed`), Products Found (`stats.products_found`).
   - Each card has: large metric number, label, and the sparkline SVG inline (`{{ sparkline_svg | safe }}`).

7. Create `admin_dashboard/templates/partials/provider_health.html`:
   - Must emit the outer `<div id="provider-health" hx-get="/admin/partials/health" hx-trigger="every 30s" hx-swap="outerHTML">` wrapper.
   - Table with columns: Provider Name, Status (with color: green/yellow/red), Success Rate, Avg Latency, Failure Count.
   - Status colors: `healthy` = `text-green-600`, `degraded` = `text-yellow-500`, `disabled` = `text-red-600`.
   - If `providers` is empty or error occurred, show "Health data unavailable" message.

8. Update `gateway.py` `create_app()`:
   - Add `from starlette.middleware.sessions import SessionMiddleware` and `import config`.
   - Add `app.add_middleware(SessionMiddleware, secret_key=config.ADMIN_SESSION_SECRET, session_cookie="admin_session", max_age=86400, same_site="lax", https_only=False)` BEFORE any route registration (middleware must be added before routes).
   - After webhook router block and BEFORE shortener catch-all, add:
     ```python
     from admin_dashboard import router as admin_router
     app.include_router(admin_router, prefix="/admin")
     logger.info("Mounted admin dashboard at /admin")
     ```
   - Update the docstring to note the new mounting order: 1. API, 2. Webhooks (optional), 3. Admin dashboard, 4. Shortener catch-all.

9. Add sidebar toggle partial route to router.py: `GET /partials/sidebar-toggle` returns the sidebar HTML with toggled visibility class (simple CSS toggle — return `<nav id="sidebar" class="fixed inset-y-0 left-0 w-64 bg-white shadow-sm block">...</nav>` or hidden version based on current state via HX-Request header). This keeps the hamburger working.

NOTE — Anti-patterns to avoid:
- Do NOT pass actual API key values to any template context
- Do NOT add `"request"` as a key in the context dict (use `request=request` keyword arg in TemplateResponse)
- Do NOT mount admin router after shortener_router
- SessionMiddleware goes on the ROOT app, not on the router
  </action>
  <verify>
    <automated>pytest tests/test_admin_web.py -k "auth or stats" -x -q</automated>
  </verify>
  <done>
    - All template files exist in correct directory structure
    - GET /admin/ with no session returns 307 redirect to /admin/login
    - GET /admin/ with valid session returns 200 with stat cards HTML
    - GET /admin/partials/stats returns HTML fragment containing stat card data (not full page)
    - GET /admin/partials/health returns HTML fragment with provider table
    - Telegram Login Widget HMAC verification passes valid data, rejects tampered/stale
    - Fallback token auth flow creates session
    - gateway.py mounts admin router before shortener catch-all
    - SessionMiddleware added with ADMIN_SESSION_SECRET
  </done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:

```bash
pytest tests/test_admin_web.py -x -q
pytest --deselect tests/test_israel_scraper.py::TestParseCccHtml::test_fallback_to_fullpage_scan -q
```

Manual smoke test (if running locally):
- Start bot: `python main.py`
- Visit http://localhost:8080/admin/ — should redirect to /admin/login
- Submit a fallback token from startup logs — should see dashboard home with stat cards
</verification>

<success_criteria>
- admin_dashboard/ module directory exists with all Python files and templates subdirectory
- GET /admin/ redirects unauthenticated users to /admin/login
- GET /admin/ returns 200 with stat cards for authenticated users
- Telegram Login Widget HMAC verification correctly accepts/rejects auth data
- Fallback token authentication works end-to-end
- HTMX polling divs present in home.html with correct hx-trigger intervals (60s stats, 30s health)
- Partial endpoints return fragments not full pages
- Provider health table shows colored status indicators
- SessionMiddleware configured on root app with ADMIN_SESSION_SECRET
- Admin router mounted before shortener catch-all in gateway.py
- pytest collects all test stubs without import errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-admin-dashboard/03-01-SUMMARY.md`
</output>
