---
phase: 05-public-web-application
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - web_app/templates/web_base.html
  - web_app/templates/home.html
  - web_app/templates/search.html
  - web_app/router.py
autonomous: false
requirements:
  - WEBA-05

must_haves:
  truths:
    - "Homepage is fully functional and readable on mobile screens"
    - "Hebrew text renders right-to-left with correct layout"
    - "English mode renders left-to-right correctly"
    - "Language toggle switches between Hebrew and English"
    - "All interactive elements (upload, tabs, buttons) are touch-friendly on mobile"
  artifacts:
    - path: "web_app/templates/web_base.html"
      provides: "Mobile-responsive base with RTL support"
      contains: "viewport"
    - path: "web_app/templates/home.html"
      provides: "Mobile-optimized upload hero with Hebrew/English text"
      contains: "text-start"
  key_links:
    - from: "web_app/templates/web_base.html"
      to: "lang query param"
      via: "dir='rtl' when lang='he'"
      pattern: "dir.*rtl"
    - from: "web_app/router.py"
      to: "lang cookie"
      via: "Cookie-based lang persistence across page loads"
      pattern: "lang"
---

<objective>
Polish the web app for mobile responsiveness and Hebrew/English RTL support. Ensure all templates use Tailwind logical properties, touch-friendly sizing, and proper Hebrew content.

Purpose: The majority of Israeli users will access this on mobile phones. RTL Hebrew support is essential for the target market. This plan ensures the app works beautifully on phones in both languages.
Output: Mobile-responsive, bilingual web app ready for user testing.
</objective>

<execution_context>
@C:/Users/leofu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leofu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-public-web-application/05-RESEARCH.md
@.planning/phases/05-public-web-application/05-01-SUMMARY.md
@.planning/phases/05-public-web-application/05-02-SUMMARY.md

<interfaces>
From web_app/templates/web_base.html (Plan 01):
- `<html lang="{{ lang }}" dir="{{ 'rtl' if lang == 'he' else 'ltr' }}">`
- Extends via {% block og_tags %}, {% block title %}, {% block content %}
- Tailwind CDN Play, HTMX, SSE extension loaded

From web_app/router.py (Plan 01):
- GET / accepts ?lang=he|en, defaults to "he"
- Lang stored in cookie for persistence
- Templates rendered with lang variable

From web_app/templates/search.html (Plan 02):
- Full result page with product cards, tabs, OG tags
- Product cards with shipping badges, price history bars, affiliate links

Tailwind logical properties (from research):
| Physical | Logical (RTL-safe) |
|----------|-------------------|
| ml-4 | ms-4 |
| mr-4 | me-4 |
| pl-4 | ps-4 |
| pr-4 | pe-4 |
| left-0 | start-0 |
| right-0 | end-0 |
| text-left | text-start |
| text-right | text-end |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Hebrew/English i18n content and RTL polish to all templates</name>
  <files>
    web_app/templates/web_base.html,
    web_app/templates/home.html,
    web_app/templates/search.html,
    web_app/router.py
  </files>
  <action>
    **1. web_app/router.py** -- Add i18n string dict and lang cookie handling:
    - Define a module-level `_STRINGS` dict with Hebrew and English translations for all user-visible text:
      ```python
      _STRINGS = {
          "he": {
              "site_title": "Amazon Photo Bot",
              "upload_title": "mza mwzrim mtmwnh",
              "upload_subtitle": "hylh tmwnh sl mwzr wnmza awtw bamazwn",
              "upload_button": "bhr tmwnh",
              "or_drag": "aw grwr tmwnh lkan",
              "how_it_works": "ayk zh ywbd?",
              "step1": "hylh tmwnh",
              "step1_desc": "zlm aw bhr tmwnh sl mwzr",
              "step2": "AI mzhh",
              "step2_desc": "hbynh hmlakwtyt mzhh at hmwzrym",
              "step3": "twzawt mamazwn",
              "step3_desc": "qbl qyswritm wmhyrym mamazwn",
              "analyzing": "mntj at htmwnh...",
              "found_products": "nmzaw {n} mwzrym...",
              "searching": "mhps bamazwn...",
              "done": "sywm!",
              "view_on_amazon": "zph bamazwn",
              "ships_to_israel": "nslh lysral",
              "likely_ships": "knrah nslh",
              "may_not_ship": "yytkn slya nslh",
              "no_results": "la nmzaw mwzrym twamym bamazwn",
              "expired": "twqp htwzawt pg",
              "not_found": "htwzawt la nmzaw",
              "reviews": "byqwrwt",
              "price_history_unavailable": "hystwryyt mhyrym la zmynh",
              "affiliate_disclosure": "kswtp Amazon, anw mrwwyhym mrkysswt mtaimwt",
              "product": "mwzr",
              "result_expired_msg": "twzawt hhypws pgw. ana hps swb.",
              "photo_too_large": "htmwnh gdwlh mdyy (mqsymwm 10MB)",
              "not_an_image": "hqwbz hyyb lhywt tmwnh",
          },
          "en": {
              "site_title": "Amazon Photo Bot",
              "upload_title": "Find Products from a Photo",
              "upload_subtitle": "Upload a photo of any product and we'll find it on Amazon",
              "upload_button": "Choose Photo",
              "or_drag": "or drag and drop here",
              "how_it_works": "How It Works",
              "step1": "Upload Photo",
              "step1_desc": "Take or select a photo of a product",
              "step2": "AI Identifies",
              "step2_desc": "Our AI identifies the products in your photo",
              "step3": "Amazon Results",
              "step3_desc": "Get links and prices from Amazon",
              "analyzing": "Analyzing photo...",
              "found_products": "Found {n} products...",
              "searching": "Searching Amazon...",
              "done": "Done!",
              "view_on_amazon": "View on Amazon",
              "ships_to_israel": "Ships to Israel",
              "likely_ships": "Likely ships",
              "may_not_ship": "May not ship",
              "no_results": "No matching products found on Amazon",
              "expired": "Results Expired",
              "not_found": "Results Not Found",
              "reviews": "reviews",
              "price_history_unavailable": "Price history unavailable",
              "affiliate_disclosure": "As an Amazon Associate, we earn from qualifying purchases",
              "product": "Product",
              "result_expired_msg": "These search results have expired. Please search again.",
              "photo_too_large": "Photo too large (max 10MB)",
              "not_an_image": "File must be an image",
          },
      }
      ```
      NOTE: The Hebrew values above are transliterated placeholders. Replace them with actual Hebrew Unicode text (copy the exact Hebrew strings from the Context decisions or use proper Hebrew characters).
    - Helper function `_get_lang(request: Request) -> str` that reads `?lang=` param first, then cookie, defaults to "he".
    - Helper function `_t(lang: str) -> dict` that returns `_STRINGS[lang]`.
    - Update all route handlers to pass `t=_t(lang)` and `lang=lang` to templates.
    - Set lang cookie in response when `?lang=` param is present.
    - Update SSE progress events to use translated strings based on lang param.
    - Update error responses in /upload to use translated strings.

    **2. web_app/templates/web_base.html** -- Mobile and RTL polish:
    - Ensure viewport meta: `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
    - Body: `<body class="bg-gray-50 min-h-screen font-sans">`
    - Header bar: simple nav with site title on start side, lang toggle on end side:
      ```html
      <header class="bg-white shadow-sm">
        <div class="max-w-4xl mx-auto px-4 py-3 flex items-center justify-between">
          <a href="/?lang={{ lang }}" class="text-lg font-semibold text-gray-800">{{ t.site_title }}</a>
          <div class="flex gap-2 text-sm">
            <a href="?lang=he" class="{% if lang == 'he' %}font-bold text-indigo-600{% else %}text-gray-500 hover:text-gray-700{% endif %}">Hebrew</a>
            <span class="text-gray-300">|</span>
            <a href="?lang=en" class="{% if lang == 'en' %}font-bold text-indigo-600{% else %}text-gray-500 hover:text-gray-700{% endif %}">English</a>
          </div>
        </div>
      </header>
      ```
    - Footer: `<footer class="max-w-4xl mx-auto px-4 py-6 text-center text-xs text-gray-400">{{ t.affiliate_disclosure }}</footer>`
    - All Tailwind classes use logical properties: `ms-`, `me-`, `ps-`, `pe-`, `text-start`, `text-end`
    - NO physical directional classes (`ml-`, `mr-`, `text-left`, `text-right`)

    **3. web_app/templates/home.html** -- Mobile-optimized landing:
    - Hero section: centered, full-width on mobile, max-w-lg on desktop
      - Title: `text-2xl sm:text-3xl font-bold text-gray-900 text-center`
      - Subtitle: `text-gray-600 text-center mt-2`
    - Upload zone: large touch target (min-height 200px), dashed border, rounded-2xl
      - `<form hx-post="/upload" hx-target="#results-area" hx-swap="innerHTML" hx-encoding="multipart/form-data">`
      - File input styled as large button: `py-4 px-8 text-lg` (touch-friendly 48px+ tap target)
      - Drag-drop text below input
      - Add minimal JS for drag-drop visual feedback:
        ```html
        <script>
        const dz = document.getElementById('dropzone');
        ['dragenter','dragover'].forEach(e => dz.addEventListener(e, ev => { ev.preventDefault(); dz.classList.add('border-indigo-500','bg-indigo-50'); }));
        ['dragleave','drop'].forEach(e => dz.addEventListener(e, ev => { ev.preventDefault(); dz.classList.remove('border-indigo-500','bg-indigo-50'); }));
        dz.addEventListener('drop', ev => { document.getElementById('photo-input').files = ev.dataTransfer.files; htmx.trigger(dz.closest('form'), 'submit'); });
        </script>
        ```
    - "How it works" section: 3-column grid on desktop (`grid md:grid-cols-3 gap-6`), stacked on mobile
      - Each step: icon/number + title + description, centered text
    - Results area: `<div id="results-area" class="mt-8"></div>`

    **4. web_app/templates/search.html** -- Update with translated strings:
    - Replace all hardcoded English text with `{{ t.xxx }}` template variables
    - Badge text: use `{{ t.ships_to_israel }}`, `{{ t.likely_ships }}`, `{{ t.may_not_ship }}`
    - "View on Amazon" button: `{{ t.view_on_amazon }}`
    - Product tabs: `{{ t.product }} {{ loop.index }}`
    - No results: `{{ t.no_results }}`
    - Expired page: `{{ t.result_expired_msg }}`
    - Ensure all card elements are touch-friendly: buttons min-height 48px, adequate spacing

    **5. web_app/templates/partials/progress.html** -- Update progress text with i18n:
    - The SSE events push translated HTML from the server (router.py handles translation)
    - Ensure progress container is mobile-friendly: centered, adequate padding
  </action>
  <verify>
    <automated>cd C:/projects/amazon-photo-bot && python -m pytest tests/test_web_app.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All templates use Tailwind logical properties (no physical directional classes)
    - Hebrew and English strings available for all user-visible text
    - Language toggle works and persists via cookie
    - Upload zone is touch-friendly (48px+ tap targets)
    - Mobile layout is single-column with appropriate sizing
    - All existing tests still pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete web application end-to-end</name>
  <action>Human verification of the complete web application flow.</action>
  <what-built>Complete public web application with photo upload, SSE progress streaming, product result cards (shipping badges, price history bars, affiliate links), multi-product tabs, Hebrew/English RTL support, and mobile-responsive layout.</what-built>
  <how-to-verify>
    1. Start the bot: `python main.py`
    2. Visit http://localhost:8080/ in a browser
    3. Verify the homepage shows an upload zone in Hebrew (RTL layout)
    4. Click the language toggle to switch to English -- verify LTR layout
    5. Switch back to Hebrew
    6. Upload a product photo (needs valid vision provider API key)
    7. Watch the SSE progress: should see 4 stages updating in real-time
    8. After completion, verify redirect to /search/{short-id} result page
    9. On result page, verify:
       - Annotated photo at top (collapsible)
       - Product tabs if multiple products detected
       - Product cards with: image, title, price, rating, shipping badge, "View on Amazon" button
       - Price history bar displayed (or "Price history unavailable" if CamelCamelCamel/Keepa unavailable)
       - OG meta tags in page source (View Source -> search for "og:image")
    10. Open the result URL on a phone or resize browser to mobile width:
        - Cards should stack vertically
        - Buttons should be easily tappable
        - Hebrew text should be right-aligned
    11. Share the result URL -- verify it loads correctly in a new browser window
    12. Try uploading a file > 10MB -- should show error message
    13. Try uploading a non-image file -- should show error message
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
# All tests pass
python -m pytest tests/test_web_app.py -x -q

# Full test suite still passes
python -m pytest -x -q

# Grep for physical directional classes (should find NONE in web_app templates)
grep -rn "ml-\|mr-\|pl-\|pr-\|text-left\|text-right" web_app/templates/ || echo "No physical classes found (GOOD)"
```
</verification>

<success_criteria>
- Homepage and result pages render correctly on mobile screens
- Hebrew content displays RTL with correct alignment
- English content displays LTR with correct alignment
- Language toggle switches between Hebrew and English
- All Tailwind classes use logical properties
- Upload zone is touch-friendly
- Human verification confirms end-to-end flow works
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-web-application/05-03-SUMMARY.md`
</output>
