---
phase: 02-enhanced-visual-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - style.py
  - tests/test_style.py
  - tests/test_price_history.py
autonomous: true
requirements:
  - PRCE-01
  - PRCE-02
  - PRCE-03

must_haves:
  truths:
    - "Product caption includes text summary of price history (ATL + 90d avg)"
    - "Product caption includes ASCII bar showing current price position in 90-day range"
    - "Product caption includes deal quality label when available"
  artifacts:
    - path: "style.py"
      provides: "render_price_bar() function and updated _price_history_line()"
      contains: "def render_price_bar"
    - path: "tests/test_price_history.py"
      provides: "Tests for render_price_bar edge cases"
      contains: "TestPriceBar"
    - path: "tests/test_style.py"
      provides: "Tests for updated _price_history_line with bar"
      contains: "price_bar"
  key_links:
    - from: "style.py::_price_history_line"
      to: "style.py::render_price_bar"
      via: "function call"
      pattern: "render_price_bar"
    - from: "style.py::product_caption"
      to: "style.py::_price_history_line"
      via: "function call"
      pattern: "_price_history_line"
---

<objective>
Add ASCII price bar rendering and enhance the price history line in product captions.

Purpose: Users see a visual bar showing where the current price sits in the 90-day range, plus a deal quality label, making it instantly clear whether the current price is good.
Output: `render_price_bar()` function in style.py, updated `_price_history_line()` that includes the bar, tests covering edge cases.
</objective>

<execution_context>
@C:/Users/leofu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leofu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-enhanced-visual-experience/02-CONTEXT.md
@.planning/phases/02-enhanced-visual-experience/02-RESEARCH.md

@style.py
@price_history.py
@tests/test_style.py
@tests/test_price_history.py

<interfaces>
<!-- Key types the executor needs -->

From price_history.py:
```python
@dataclass
class PriceHistory:
    asin:         str
    source:       str             # "camelcamelcamel" | "keepa"
    current:      Optional[float] = None
    low_all_time: Optional[float] = None
    avg_90d:      Optional[float] = None
    avg_30d:      Optional[float] = None
    low_90d:      Optional[float] = None

    @property
    def deal_label(self) -> str:
        # Returns "All-time low" / "Great deal" / "Below avg" / ""
```

From style.py (current _price_history_line):
```python
def _price_history_line(ph) -> str:
    """Returns: "\n📊 ATL $24\\.99 · 90d avg $38\\.50 · ✅ Below avg" """
```

From style.py:
```python
def esc(text: str) -> str:
    """Escape MarkdownV2 special chars."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add render_price_bar() and test suite</name>
  <files>style.py, tests/test_price_history.py</files>
  <behavior>
    - render_price_bar(PriceHistory with current=112, low_90d=89, avg_90d=149) returns multi-line string with bar, pointer, and deal label
    - render_price_bar(PriceHistory with current=low_90d) returns bar with pointer at left edge
    - render_price_bar(PriceHistory with current=avg_90d) returns bar with pointer at right edge
    - render_price_bar(PriceHistory with current=None) returns empty string
    - render_price_bar(PriceHistory with low_90d=None, low_all_time=50) uses low_all_time as fallback
    - render_price_bar(PriceHistory with low_90d=100, avg_90d=100) handles equal range (synthetic range)
    - Bar output uses Unicode block chars and dash chars, contains $ price labels
  </behavior>
  <action>
    1. Create `TestPriceBar` class in `tests/test_price_history.py` with test cases from behavior spec above. Tests import `render_price_bar` from `style` module. Each test creates a PriceHistory fixture and asserts expected output properties (contains $ amounts, correct deal label, non-empty bar chars).

    2. Add `render_price_bar(ph: PriceHistory, bar_width: int = 10) -> str` to `style.py`. Implementation per research Pattern 4:
       - Use `low_90d` (fallback `low_all_time`) as range low
       - Use `avg_90d` as range high (fallback `current * 1.3` for synthetic range)
       - Calculate ratio = (current - low) / (high - low), clamped 0.0-1.0
       - Render filled blocks, optional partial block, empty dashes
       - Format: `$89 ████████── $149` line 1, `     ^ $112 now` line 2, deal_label line 3
       - Return empty string if current is None or range is invalid
       - Do NOT escape for MarkdownV2 here -- the caller (_price_history_line) handles escaping

    Run tests after implementation to verify GREEN.
  </action>
  <verify>
    <automated>pytest tests/test_price_history.py -x -k "TestPriceBar" -v</automated>
  </verify>
  <done>render_price_bar returns correct ASCII bar for all edge cases; all TestPriceBar tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update _price_history_line() to include price bar</name>
  <files>style.py, tests/test_style.py</files>
  <action>
    1. Update `_price_history_line(ph)` in `style.py` to include the ASCII price bar:
       - Keep existing ATL + 90d avg summary line (first line, prefixed with 📊)
       - Add render_price_bar() output below the summary, each line escaped with `esc()` for MarkdownV2
       - Wrap bar lines in monospace backtick block so Unicode chars render properly: `` `$89 ████████── $149` ``
       - Include deal_label on its own line if present (already handled by render_price_bar, but ensure MarkdownV2 escaping)
       - Total price history section should be 3-4 lines max
       - Return empty string if ph is None or has no useful data (existing behavior preserved)

    2. Add/extend tests in `tests/test_style.py` for `_price_history_line`:
       - Test that output contains bar characters when PriceHistory has current + low_90d + avg_90d
       - Test that output still works (summary only, no bar) when PriceHistory has only low_all_time (no low_90d, no avg_90d)
       - Test that output is empty string when ph is None

    3. Verify that `product_caption` still respects the 1024 char limit -- the added bar lines add ~80 chars. Check the truncation logic in product_caption handles the extra lines gracefully.
  </action>
  <verify>
    <automated>pytest tests/test_style.py tests/test_price_history.py -x -v</automated>
  </verify>
  <done>_price_history_line includes ASCII bar when data available; product_caption stays under 1024 chars; all style and price history tests pass</done>
</task>

</tasks>

<verification>
- `pytest tests/test_style.py tests/test_price_history.py -x` passes
- `render_price_bar` produces correct output for normal, edge, and missing-data cases
- `_price_history_line` includes bar when data exists, omits when not
- No MarkdownV2 parse errors in formatted output
</verification>

<success_criteria>
- render_price_bar() exists and handles all PriceHistory variants (normal, missing fields, equal range)
- _price_history_line() includes ASCII bar below the summary text
- deal_label from PriceHistory.deal_label property appears in output when applicable
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-enhanced-visual-experience/02-01-SUMMARY.md`
</output>
